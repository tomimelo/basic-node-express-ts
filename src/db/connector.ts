// import { connect } from 'ODM/ORM';

const options = {
    //Connection options
}

export default {
    // createConnection: async () => connect(options) //Uncomment this line when db connection configured properly
}